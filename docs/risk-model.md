# Planned Risk Assessment Model

The full framework will connect asset criticality to threats and vulnerabilities.

Workflow:

`Asset → Threat → Vulnerability → Likelihood → Impact → Risk → Mitigation`

Planned formula:

`Risk Score = Likelihood × Impact`

Likelihood and impact will use 1–5 scales. Final thresholds will be documented as project assumptions and validated through controlled case studies.
